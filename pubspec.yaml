name: robot_calendar
description: A new Flutter project.

publish_to: 'none' # Remove this line if you are planning to publish your package.

version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  table_calendar: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/robot1.png
    - assets/box_background.png
    - assets/check.png
    - assets/cross.png

